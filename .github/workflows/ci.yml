name: Pipeline

on:
  push:
    branches:
      - main
    tags:
      - '*'

permissions:
  contents: write
  packages: write

jobs:
  run-build:
    uses: "oezcan-guezelsarslan/cicdtemplates/.github/workflows/ci-maven.yml@v1.1.0"
    permissions:
      contents: write
      packages: write
      security-events: write
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}